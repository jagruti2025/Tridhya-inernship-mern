//Installation

//Install globally(available anywhere on your system)
//npm install -g typescript

//Install locally per-project
// npm init -y          # creates package.json
// npm install typescript --save-dev

//tsconfig.json
//Generated via `tsc --init`

//Compilation
//tsc → reads .ts → outputs .js

//Project Structure
//src/ (rootDir)
//dist/ (outDir)
//.tsx for React components

//npm install typescript --save-dev (Install TS locally in project)
//npx tsc --init (Generate tsconfig.json)
//npx tsc (Compile once)
//npx tsc --watch (Auto-compile on save)
//node dist/index.js (Run compiled JS)
// "strict": true (Enable all strict checks)
